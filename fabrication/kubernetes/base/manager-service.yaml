apiVersion: v1
kind: Service
metadata:
  name: agent-manager
  namespace: maze-prod
spec:
  selector:
    app: agent-manager
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
